dependencies {
    compile project(':core')
    compile group: 'junit', name: 'junit', version: '4.12'
}

test {
    ignoreFailures = true
}

jar{
    from {
        configurations.compile.findAll {!it.name.contains('junit')}
                .collect { it.isDirectory() ? it : zipTree(it)}
    }
}